box: ubuntu:15.04
build:
    steps:
        - script:
            name: install wget
            code: |
              sudo apt-get update -y
              sudo apt-get install -y wget
        - poppen/chefdk:
            version: 0.10.0
        - script:
            name: version
            code: |
              thor list
              thor 'version:current'
